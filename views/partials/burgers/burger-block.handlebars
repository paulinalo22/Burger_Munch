{{!-- Each burger must be generated with a button that says
"MUNCH IT" --}}

<li>
	{{burger_name}}

	{{!-- should only show button if burger is not devoured --}}
	<button class="change-devour" data-id="{{id}}">
		MUNCH IT!
		{{!-- don't think we need an else/if because it won't switch back and forth --}}
	</button>
</li>